import { Text, View } from "react-native";

export default function MessageThreadScreen() {
  return (
    <View testID="message-thread">
      <Text>message-thread — placeholder</Text>
    </View>
  );
}
